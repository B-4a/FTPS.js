pixels = document.addQuerySelector('./container');

const pixels = new int[4*x*y];

pixels[0] = xRed (Iterations ++ function (green){2.0f,1.0f,0.2f};

float[0] = 1.0f{yGreen};
short[0] =0.0f {doubleGreen});
pixels.setPixels('green');
 
console log('${Red}$');
pixels = document.addQuerySelector('.p:Input');

const pixels = new int[4*x*y];

pixels[0] = xRed (Iterations ++ function (Red){1.0f,0.0f,0.2f};

float[0] = 1.0f{yRed};
short[0] =0.0f {doubleRed});
pixels.setPixels('red');
 
console log('${Red}$');

