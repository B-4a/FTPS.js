pixels[3] = document.addQuerySelector('./container');

const pixels = new int[1/2*x*y];

pixels[3] = yGreen(Iterations ++ function (green){2.0f,1.0f,0.2f};

float[3] = 1.0f{yGreen};
short[3] =0.0f {doubleGreen});
pixels.setPixels('green');
 
console log('${green}$');

jsonArrData.onClick switch(set) {

 (pixels.int)> i this.a

   render [{ case:[2]

  ]};
  return ({`set`});


pixels[2]= document.addQuerySelector('./require');

const pixels = new int[1/2*x*y];

pixels[2] = xRed (Iterations ++ function (Red){1.0f,0.0f,0.2f};

float[2] = 0.2f{yRed};
short[2] =0.2f {doubleRed});
pixels.setPixels('red');
 
console log('${Red}$');

