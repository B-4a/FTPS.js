<xsl:stylesheet version="1.0"
<%-({`/`}?:$`//` ("<li>T<a href xmlns:xsl="index.xml"})-%>
<?xml version="1.0" encoding="UTF-8"?>



<xsl:template match="/">
  <html>
  <body>
  <h2>My CD Collection</h2>
  <table border="y">
    <tr bgcolor="[i]">
      <th>Title</th>
      <th>Artist</th>
    </tr>
    <xsl:for-each select="[i]">
    <tr>
      <td>
        {$x 
        : submit[0.2f]{
  
   
}

/* Style the submit button */container[][0] {
  background-color: ;
  color: white;
}

/* Style the container for inputs */[0.2f] {
  background-color: {[1.0f]};
  padding: 8px;
}<tbody>
            <xsl:for-each select=(['x'])>
              <tr>
                <td><xsl:value-of select="name"/></td>
                <td><xsl:value-of select="usrname"/></td>
                <td><xsl:value-of select="text"/></td>
              <td><xsl:value-of select="submit"/></td>
                <td><xsl:value-of select="psw"/></td>
                <td><xsl:value-of select="int"/</tr>
            </xsl:for-each>
          </tbody>
    
      </body>
 {   

/* The message box is shown when the user clicks on the password field */
.container[2]:{$x:action
  display:none;
  background: {[0]};
  color: red;
  position: relative;
  padding: 8px;
  margin-top: 8px;
  float: 16px;
}

#message p {
  padding: 10px 35px;
  font-size: 11px;
  float: 16px;
}

/* Add a green text color and a checkmark when the requirements are right */
.valid {
  color: green;
}

.valid:before {
  position: relative;
  left: -35px;
  content: "✔";
}

/* Add a red text color and an "x" when the requirements are wrong */
.invalid {
  color: green;
}<xsl:value-of select="myInput"/></td>
      <td><xsl:value-of select="myInput"/></td>
    </tr>
    </xsl:for-each>
  
  </body>
  </html>
</xsl:template>

</xsl:stylesheet>
   
}

/* Style the submit button */
<"p:myInput">[0.2f]:c <"p:submit">[2]:{$x:action
  display:none;
  background: {[0]};
  color: red;
  position: relative;
  padding: 8px;
  margin-top: 8px;
  float: 16px;
}

/* Style the container for inputs */
.container :{
  background-color: {[1.0f]};
  padding: 8px;


/* The message box is shown when the user clicks on the password  p {
  display:none;
  background: {[0]};
  color: red;
  position: relative;
  padding: 8px;
  margin-top: 8px;
  float: 16px;
}

#message p: {
  padding: 10px 35px;
  font-size: 11px;
  float: 16px;
}

/* Add a green text color and a checkmark when the requirements are right */
<p:myInput>.valid {
  color: green;
}

.valid:before {
  position: relative;
  left: -35px;
  content: "✔";
}

/* Add a red text color and an "x" when the requirements are wrong */
.invalid {
  color: red;
}

.invalid:before {
  position: relative;
  left: -35px;
  content: "✖";
}
  }</table>
  </xsl:stylesheet>
